<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anwei.box.modules.system.user.mapper.UserMapper">

    <select id="mapperTest" resultType="com.anwei.box.modules.system.user.entity.User">
        SELECT * FROM sys_user WHERE org_id=2 AND username='Anwei' AND delete_flag=0 and 1=1
    </select>

</mapper>
